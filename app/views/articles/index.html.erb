<% provide :title, 'メニュー一覧' %>

<div class='container'>
  <h2>メニュー一覧</h2>
  <%= render 'search' %>

  <div class='article'>
  <% if @search_articles %>
    <%= render @search_articles %>
  <% else %>
    <%= render @articles %>
  <% end %>
  </div>
</div>